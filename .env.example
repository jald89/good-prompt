### Example environment variables (do NOT store secrets here)
### Copy this file to `.env.local` or `.env` and fill in real values for local development.

### Client (exposed to browser via Vite)
# URL base for frontend API calls
VITE_API_BASE_URL=http://localhost:3000
# N8N webhook - keep private on the server (do NOT set a VITE_ var for this)
N8N_SERVER_URL=
# API_MODE: 'server' | 'n8n' (when 'n8n' the server will forward requests to N8N_SERVER_URL)
API_MODE=server
N8N_SERVER_URL=https://autos.djibia.cloud/webhook-test/a111e18a-2a92-4659-96dd-d7ec0ebc861a
API_MODE=n8n  ## server | n8n

### Server/runtime
# Port the Express server listens on
PORT=3000

# CORS: comma-separated list of allowed origins (no trailing slash), use '*' to allow all
ALLOWED_ORIGINS=http://localhost:5173

# NODE_ENV=development | production
NODE_ENV=development

# Session or signing secret (change before production)
SESSION_SECRET=your-session-secret

# Example API key placeholders (do NOT commit real keys)
OPENAI_API_KEY=

# Notes:
# - Keep secrets in `.env.local` (ignored) or in your host/CI secret manager.
# - After editing `.env`/`.env.local`, restart your dev servers.
